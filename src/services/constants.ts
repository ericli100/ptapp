// An arbitrary string that no user will every likely use for search.
// This is used to avoid the inital multiple rednering caused by search

// eslint-disable-next-line import/prefer-default-export
export const STRING_NEVER_USED_FOR_SEARCH = ')(#J:*(&*$#_)#$@#$HDLSF';
